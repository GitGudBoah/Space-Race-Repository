//portrait
speaker = spr_com_glaucus;

//text
text = "Space is humanity's last hope - yet if it weren't for these walls, we'd all be dead. Ironic, isn't it?";
boxWidth = sprite_get_width(spr_textbox);
stringHeight = string_height(text);

counter = 0;